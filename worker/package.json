{
  "name": "kadoze-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply kadoze --local",
    "db:migrate": "wrangler d1 migrations apply kadoze --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260524.0",
    "node-addon-api": "^8.8.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "add": "^2.0.6",
    "node-gyp": "^12.3.0"
  }
}
